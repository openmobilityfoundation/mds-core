suite: test deployment all default
templates:
  - deployment.yaml
tests:
  - it: all docs
    values:
      - ./values-namespace.yaml
    asserts:
      - hasDocuments:
          count: 7
      - equal:
          path: metadata.namespace
          value: my-namespace
      