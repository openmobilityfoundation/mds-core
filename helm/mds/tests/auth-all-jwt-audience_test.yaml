suite: test authetication jwt audience
templates:
  - auth.yaml
tests:
  - it: doc 0
    release:
      namespace: mds
    values:
      - ./auth-values-jwt-audience.yaml
    asserts:
      - hasDocuments:
          count: 7