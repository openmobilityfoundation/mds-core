suite: test authetication jwt audience audit
templates:
  - auth.yaml
tests:
  - it: doc 2
    release:
      namespace: mds
    set:
      apis:
        - jwtAudience: provider
        - jwtAudience: agency
        - name: mds-audit
          pathPrefix: /audit
          port: 4002
          version: latest
          useDB: true
          useCache: true
          useEvents: true
          jwtAudience: audit
        - jwtAudience: policy
        - jwtAudience: compliance
        - jwtAudience: daily
        - jwtAudience: native
      jwt:
        issuer: my-issuer
    asserts:
      - isAPIVersion:
          of: authentication.istio.io/v1alpha1
        documentIndex: 2
      - isKind:
          of: Policy
        documentIndex: 2
      - equal:
          path: metadata.name
          value: mds-audit-auth
        documentIndex: 2
      - equal:
          path: metadata.namespace
          value: mds
        documentIndex: 2
      - equal:
          path: spec.targets[0].name
          value: mds-audit
        documentIndex: 2
      - isEmpty:
          path: spec.peers[0].mtls
        documentIndex: 2
      - equal:
          path: spec.origins[0].jwt.audiences[0]
          value: "audit"
        documentIndex: 2
      - equal:
          path: spec.origins[0].jwt.issuer
          value: "my-issuer"
        documentIndex: 2
      - equal:
          path: spec.origins[0].jwt.jwksUri
          value: my-issuer.well-known/jwks.json
        documentIndex: 2
      - equal:
          path: spec.origins[0].jwt.trigger_rules[0].excluded_paths[0].exact
          value: /audit/health
        documentIndex: 2
      - equal:
          path: spec.origins[0].jwt.trigger_rules[0].excluded_paths[1].exact
          value: /health
        documentIndex: 2
      - equal:
          path: spec.principalBinding
          value: USE_ORIGIN
        documentIndex: 2
