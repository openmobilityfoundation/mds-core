suite: test deployment all default
templates:
  - deployment.yaml
tests:
  - it: all docs
    release:
      namespace: mds
    values:
      - ./deployment-values-namespace.yaml
    asserts:
      - hasDocuments:
          count: 7
      - equal:
          path: metadata.namespace
          value: mds
