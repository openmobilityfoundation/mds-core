{
  "name": "@container-images/mds-ingest-migration-processor",
  "version": "0.0.13",
  "private": true,
  "description": "Docker Image for MDS Indest Data Migration Processor",
  "keywords": [
    "mds"
  ],
  "license": "Apache-2.0",
  "author": "City of Los Angeles",
  "main": "dist/index.js",
  "files": [
    "dist/"
  ],
  "scripts": {
    "bundle": "pnpm bundle:production",
    "bundle:development": "webpack --mode=development --env npm_package_name=${npm_package_name} --env npm_package_version=${npm_package_version}",
    "bundle:production": "webpack --mode=production --env npm_package_name=${npm_package_name} --env npm_package_version=${npm_package_version}",
    "image": "pnpm image:production",
    "image:development": "pnpm bundle:development && ../../bin/build-helper buildImage mds-ingest-migration-processor:latest",
    "image:production": "pnpm bundle:production && ../../bin/build-helper buildImage mds-ingest-migration-processor:${npm_package_version}"
  },
  "dependencies": {
    "@mds-core/mds-ingest-migration-processor": "workspace:0.0.12",
    "@mds-core/mds-webpack-config": "workspace:0.1.3"
  }
}
