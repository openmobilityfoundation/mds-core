suite: test deployment all no-db
templates:
  - deployment.yaml
tests:
  - it: all docs
    values:
      - ./values-no-db.yaml
    release:
      namespace: mds
    asserts:
      - hasDocuments:
          count: 7
