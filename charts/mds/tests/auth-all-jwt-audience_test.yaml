suite: test authetication jwt audience
templates:
  - auth.yaml
tests:
  - it: doc 0
    values:
      - ./auth-values-jwt-audience.yaml
    release:
      namespace: mds
    asserts:
      - hasDocuments:
          count: 7