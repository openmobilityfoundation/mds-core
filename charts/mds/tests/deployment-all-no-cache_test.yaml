suite: test deployment all no-cache
templates:
  - deployment.yaml
tests:
  - it: all docs
    values:
      - ./deployment-values-no-cache.yaml
    release:
      namespace: mds
    asserts:
      - hasDocuments:
          count: 7
