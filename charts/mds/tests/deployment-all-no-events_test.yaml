suite: test deployment all no-events
templates:
  - deployment.yaml
tests:
  - it: all docs
    values:
      - ./values-no-events.yaml
    release:
      namespace: mds
    asserts:
      - hasDocuments:
          count: 7
