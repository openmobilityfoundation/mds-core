suite: test deployment all default
templates:
  - deployment.yaml
tests:
  - it: all docs
    values:
      - ./deployment-values-namespace.yaml
    release:
      namespace: mds
    asserts:
      - hasDocuments:
          count: 7
      - equal:
          path: metadata.namespace
          value: mds
